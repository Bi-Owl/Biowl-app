feat: Implement user activation and enhance signup flow

- **Backend:**
  - Added `isActive` field to `User` model with `defaultValue: false`.
  - Modified login logic to prevent inactive users from authenticating.
  - Hardened `adminController.updateUser` to explicitly update allowed fields.
- **Frontend:**
  - Login form now displays a toast notification for inactive accounts.
  - Admin user table displays user activation status.
  - Admin user edit modal allows toggling user activation status using a new `BaseToggle` component.
  - Enhanced signup flow: Removed automatic login/redirect and added a 500ms delay before page reload to show success toast.